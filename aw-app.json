{
  "manifest_version": 1,
  "id": "plaud",
  "name": "Plaud",
  "version": "0.2.1",
  "description": "Browse your Plaud AI recorder notes without leaving the workspace: see every recording, play or download the original audio, and read the transcript and AI summary \u2014 plus the 5 aw-plaud MCP tools an agent can use to pull the same data.",
  "icon": "mic",
  "tier": "inprocess",
  "requires_ui_refresh": true,
  "publisher": "TekFlox",
  "resource_estimate": {
    "cpu": "low",
    "memory": "low",
    "disk": "low"
  },
  "runtime": {
    "python": ">=3.11",
    "entrypoint": "plaud_app.plugin:PlaudAppPlugin",
    "pip_requires": [
      "httpx>=0.24"
    ]
  },
  "permissions": [
    "routes:register",
    "secrets:own",
    "ui:code"
  ],
  "contributes": {
    "routes": [
      {
        "prefix": "/api/apps/plaud"
      }
    ],
    "windows": [
      {
        "id": "plaud.main",
        "title": "Plaud",
        "icon": "mic",
        "body": {
          "type": "component"
        }
      }
    ],
    "frontend": {
      "mode": "component",
      "bundle": "ui/dist/plaud-ui.mjs",
      "slots": [
        "core.window.body:plaud.main"
      ]
    },
    "mcp": {
      "reload_on_save": true,
      "provides": [
        "plaud_status",
        "plaud_list_recordings",
        "plaud_get_transcript",
        "plaud_get_summary",
        "plaud_download_audio"
      ]
    },
    "skills": [
      {
        "id": "aw-plaud",
        "path": "skills/aw-plaud/SKILL.md",
        "description": "Personal Plaud account connector \u2014 tool reference, the Apps \u203a Plaud UI, how to obtain/refresh the bearer token, and known limitations."
      }
    ]
  },
  "config_schema": {
    "type": "object",
    "properties": {
      "plaud_bearer_token": {
        "type": "string",
        "x-secret": true,
        "description": "Bearer token lifted from a logged-in web.plaud.ai browser session (DevTools \u2192 Network \u2192 any api.plaud.ai request \u2192 authorization header). Never stored in plain config \u2014 routed to the secret store via POST /api/apps/plaud/settings, same pattern as aw-app-git's github_token and aw-app-notion's notion_token. Short-lived JWT (~24h) with no refresh flow \u2014 re-paste when it expires."
      }
    },
    "required": []
  },
  "config_visible": false,
  "dependencies": {
    "apps": [
      {
        "id": "mcp-gateway",
        "version": ">=0.1.0",
        "required": true,
        "reason": "This app contributes its own mcp.json (the aw-plaud server) that MCP Gateway discovers and merges."
      }
    ]
  },
  "migrations": {}
}
